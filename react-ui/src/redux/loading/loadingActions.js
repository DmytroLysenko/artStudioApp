import { createAction } from "@reduxjs/toolkit";

// TODO: Remove if this not need
export const setLoading = createAction("loading/set");
export const unsetLoading = createAction("loading/unset");
